# Basic steps for installation

npm init
create package.json
npm install mongodb mongoose config elasticsearch --save

app.js is the entry point of the applications
indexer.js is having all the code.

